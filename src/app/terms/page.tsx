export default function TermsPage() {
  return (
    <main className="mx-auto max-w-2xl px-4 py-12">
      <h1 className="serif text-4xl">Terms</h1>
      <p className="mt-4 text-ink-soft">
        New listings get two months free, then a monthly subscription to keep Call now. Customers ring the
        professional’s own number and say they found them on 1mileaway. Each of those calls is emailed and counted so
        the professional can see whether the listing is worth paying for. There is no per-lead invoice and no prepaid
        wallet.
      </p>
    </main>
  );
}
