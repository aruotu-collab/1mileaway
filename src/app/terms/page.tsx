export default function TermsPage() {
  return (
    <main className="mx-auto max-w-2xl px-4 py-12">
      <h1 className="serif text-4xl">Terms</h1>
      <p className="mt-4 text-ink-soft">
        Professionals are charged only for qualified leads after any free trial. Missed or “not a job” calls are not
        billed. A professional may have at most one unpaid trust lead at a time. Listings remain visible if a lead is
        outstanding, but Available Now is paused.
      </p>
    </main>
  );
}
