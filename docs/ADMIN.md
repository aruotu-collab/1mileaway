# Admin

`/admin` is a server-authorised control centre. Super admin is granted when `aruotu@gmail.com` (or any `SUPER_ADMIN_EMAILS` address) signs in, and the role is stored on `profiles`. Client-side email checks are not used.

Use Admin to inspect professionals (including ranking explanation), leads, calls, payments, trial balances, country activation, emails, and audit events.
